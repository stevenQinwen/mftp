<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.iquantex.mftp.dao.CustInfoDao">

	<select id="selectCustAgeDistributed" parameterType="string" resultType="java.util.HashMap">
		${sql} 
	</select>
	<select id="selectCustSexDistributed" parameterType="string" resultType="java.util.HashMap">
		${sql} 
	</select>
	

</mapper>