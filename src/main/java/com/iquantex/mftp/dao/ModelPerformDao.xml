<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.iquantex.mftp.dao.ModelPerformDao">

	<select id="select_next7day_predicted" parameterType="string" resultType="java.util.HashMap">
		${sql} 
	</select>
	
	
	
	<select id="selectModel_performRecent_Nday" parameterType="string" resultType="modelPerform">
		${sql} 
	</select>
	

</mapper>